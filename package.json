{
  "name": "lucid-fetch-helper",
  "version": "2.0.0",
  "description": "get all data for certain database model with pagination ,filtration ,date range ,sort by column and search query all in one request",
  "main": "lib/index.js",
  "types": "lib/index.d.ts",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Eduact-me/lucid-fetch-helper.git"
  },
  "homepage": "https://github.com/Eduact-me/lucid-fetch-helper#readme",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc",
    "format": "prettier --write \"src/**/*.ts\"",
    "lint": "tslint -p tsconfig.json",
    "prepare": "npm run build",
    "prepublishOnly": "npm run lint",
    "preversion": "npm run lint",
    "version": "npm run format && git add -A src",
    "postversion": "git push && git push --tags"
  },
  "keywords": [
    "lucid",
    "adonis",
    "helper",
    "orm"
  ],
  "author": "mohammed_essam",
  "license": "ISC",
  "files": [
    "lib/**/*"
  ],
  "dependencies": {
    "dayjs": "^1.10.4"
  },
  "devDependencies": {
    "prettier": "^2.3.0",
    "tslint": "^6.1.3",
    "tslint-config-prettier": "^1.18.0",
    "typescript": "^4.2.4"
  }
}
